Due tipi di script: 
    + .m: funziona come un .c
    + .mlx: live script
        si possono eseguire determinate caselle di codice

Matlab non è fortemente tipizzato

e.g.    x = 0;
        x = "ciao";

la variabile viene salvata nel workspace
è possibile salvare il workspace usando il comando save. lo salva il formato .mat -> save ("nomeWorkspace")
per pulire il workspace si usa il comando clear
per caricare un workspace si usa load nomeWorkspace

ans è una variabile già usata da MatLab e mostra l'ultimo risultato non assegnato
    e.g.    1   3 + 1

Matlab usa i come variabile immaginaria
idem j
pi è i p greco

+---------------------------------------------------------+
14/03/2024
